export const writeSearch = (input) => ({
  type: 'WRITE_SEARCH',
  input
});

export const submitSearch = (submitted) => ({
  type: 'SUBMIT_SEARCH',
  submitted
});
export const setMap = (map) => ({
  type: 'SET_MAP',
  map
});
